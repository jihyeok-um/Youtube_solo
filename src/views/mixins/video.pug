mixin video(video)
    a(href=`/videos/${video.id}`)
        div=video.title
    p=video.description
    ul 
        each hashtag in video.hashtags
            li=hashtag 
    small=video.createdAt
    hr